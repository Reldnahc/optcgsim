---
spec_version: "v6"
spec_package_name: "optcg-md-specs-v6"
doc_id: "11-testing-quality"
doc_title: "Testing Quality"
doc_type: "spec-section"
status: "canonical"
machine_readable: true
---

# Testing and Quality Strategy
<!-- SECTION_REF: 11-testing-quality.s001 -->
Section Ref: `11-testing-quality.s001`

## Testing goals
<!-- SECTION_REF: 11-testing-quality.s002 -->
Section Ref: `11-testing-quality.s002`

The engine must be testable without UI, network, Redis, or Postgres. Most correctness bugs should be caught in package-level tests before an integration test or manual playtest.

## Test pyramid
<!-- SECTION_REF: 11-testing-quality.s003 -->
Section Ref: `11-testing-quality.s003`

```text
              manual playtests
          end-to-end browser tests
       match-server integration tests
    golden replay + hidden-info tests
  engine interaction + card tests
DSL primitive + invariant + fuzz tests
```

## Unit tests per DSL primitive
<!-- SECTION_REF: 11-testing-quality.s004 -->
Section Ref: `11-testing-quality.s004`

Every primitive has tests independent of specific cards:

- `draw`
- `ko`
- `trash`
- `bounce`
- `search`
- `lookAtTop`
- `modifyPower`
- `modifyCost`
- `giveKeyword`
- `replacement`
- `damage`
- `addLife`
- `attachDon`
- `returnDon`
- `choice`
- `conditional`
- `sequence`

Primitive tests should assert events, state, decisions, and visibility where applicable.

## Unit tests per card
<!-- SECTION_REF: 11-testing-quality.s005 -->
Section Ref: `11-testing-quality.s005`

Every implemented non-vanilla card gets a test file.

```text
tests/cards/
  OP01-001.test.ts
  OP01-015.test.ts
  OP01-040.test.ts
```

Minimum assertions:

- Effect appears in legal actions or trigger queue at correct timing.
- Required costs are enforced.
- Optional effects can be declined.
- Legal targets are correct.
- Effect resolves correctly.
- Edge cases: no targets, insufficient cost, source moved, once-per-turn used.
- Expected events are emitted.

## Custom handler tests
<!-- SECTION_REF: 11-testing-quality.s006 -->
Section Ref: `11-testing-quality.s006`

Every custom handler requires:

- Basic effect test.
- Edge-case tests.
- Interaction tests with relevant keywords or timing.
- Determinism test if choices or RNG are involved.
- Regression test for any bug found.

## Interaction tests
<!-- SECTION_REF: 11-testing-quality.s007 -->
Section Ref: `11-testing-quality.s007`

Representative interactions:

```text
tests/interactions/
  blocker-plus-unblockable.test.ts
  double-attack-plus-banish.test.ts
  replacement-on-ko.test.ts
  simultaneous-ko-triggers.test.ts
  on-ko-source-presence.test.ts
  trigger-during-damage-defers.test.ts
  event-activates-effect-after-resolution.test.ts
  negative-power-stays-on-field.test.ts
  negative-cost-clamps-to-zero.test.ts
```

## Invariant tests
<!-- SECTION_REF: 11-testing-quality.s008 -->
Section Ref: `11-testing-quality.s008`

Run after every action, decision response, effect resolution, and replay step in test mode.

```ts
assertAllCardsInExactlyOneLocation(state);
assertNoDuplicateInstanceIds(state);
assertCharacterAreaSizeAtMostFive(state);
assertStageAreaSizeAtMostOne(state);
assertLeaderAreaExactlyOne(state);
assertAttachedDonConsistency(state);
assertNoIllegalHiddenInfoInViews(state);
assertPendingDecisionIsValid(state);
assertEffectQueueEntriesAreResolvableOrCancelled(state);
assertStateHashStable(state);
```

## Fuzz tests
<!-- SECTION_REF: 11-testing-quality.s009 -->
Section Ref: `11-testing-quality.s009`

Use random legal actions generated by `getLegalActions()`.

```ts
for (let seed = 0; seed < 1000; seed++) {
  let state = createRandomLegalGame(seed);

  for (let i = 0; i < 500 && !isGameOver(state); i++) {
    const player = currentDecisionPlayerOrTurnPlayer(state);
    const legal = getLegalActions(state, player);
    const action = chooseRandom(legal, seed);
    const result = applyAction(state, action);
    assertInvariants(result.state);
    state = result.state;
  }
}
```

Fuzzing catches more state-machine bugs than hand-written tests.

## Golden replay tests
<!-- SECTION_REF: 11-testing-quality.s010 -->
Section Ref: `11-testing-quality.s010`

Store known game scripts and final state hashes.

```text
fixtures/replays/
  vanilla-basic-game.json
  blocker-counter-basic.json
  double-attack-life-trigger.json
  simultaneous-ko-triggers.json
  replacement-trigger-trash.json
```

CI replays them and checks:

- Final state hash.
- Checkpoint hashes.
- Event count/type sequence, optionally.
- No hidden-info leak in generated views.

## Hidden-information tests
<!-- SECTION_REF: 11-testing-quality.s011 -->
Section Ref: `11-testing-quality.s011`

For every view-filtering change:

- Assert opponent hand card IDs absent.
- Assert deck order absent.
- Assert face-down life absent.
- Assert RNG state absent.
- Assert effect queue internals absent.
- Assert temporary reveals are recipient-filtered.
- Assert declined triggers never reveal card IDs.
- Assert spectator policy applied.

Create fixtures for each reveal scenario.

## Replay drift tests
<!-- SECTION_REF: 11-testing-quality.s012 -->
Section Ref: `11-testing-quality.s012`

Whenever engine or effect definitions change:

1. Replay previous golden logs under the intended version bundle.
2. Compare checkpoint hashes.
3. Fail CI on unexpected drift.
4. Require migration/version-pin note for intentional drift.

## Protocol tests
<!-- SECTION_REF: 11-testing-quality.s013 -->
Section Ref: `11-testing-quality.s013`

Match-server tests:

- Duplicate `clientActionId` returns same result.
- Stale `expectedStateSeq` rejected.
- Wrong player action rejected.
- Decision response with wrong `decisionId` rejected.
- Reconnect returns current filtered view.
- Disconnect timer behavior.
- Ranked queue-created matches stamp `gameType`, `formatId`, and `ladderId` correctly.
- Custom lobby join rejects incorrect password and unauthorized spectator mode requests.
- Spectator cannot send actions.

## Database tests
<!-- SECTION_REF: 11-testing-quality.s014 -->
Section Ref: `11-testing-quality.s014`

- Deck card variants can coexist.
- Total base-card quantity validation catches over-limit variants.
- Active ban query works for permanent and temporary bans.
- Replay rows require version fields.
- Match rows preserve `game_type`, `format_id`, and optional `ladder_id`.
- Ladder-scoped rating uniqueness works per `(user_id, ladder_id)`.
- Rating history records Elo delta and reason.
- Disconnect discipline event queries surface active lockouts.
- Match status indexes support expected queries.

## Performance tests
<!-- SECTION_REF: 11-testing-quality.s015 -->
Section Ref: `11-testing-quality.s015`

Before public launch:

- Max effect queue depth under fuzz tests.
- Average `computeView()` cost.
- Average `filterStateForPlayer()` cost.
- Serialized `PlayerView` size.
- Match server memory per active match.
- Replay log size per action.

## Coverage gates
<!-- SECTION_REF: 11-testing-quality.s016 -->
Section Ref: `11-testing-quality.s016`

Suggested early gates:

- 90%+ line coverage in `engine-core` for functions excluding generated card data.
- 100% of implemented non-vanilla cards have at least one test.
- 100% of custom handlers have direct tests.
- 0 stale source text hashes in ranked card pool.
- 0 unsupported cards allowed in ranked validation fixtures.
- 0 queue-eligible ranked formats missing ladder configuration.

## Manual playtest checklist
<!-- SECTION_REF: 11-testing-quality.s017 -->
Section Ref: `11-testing-quality.s017`

For each release candidate:

- Complete match without errors.
- Reconnect both players mid-match.
- Join one unranked queue match and one passworded custom lobby match.
- Trigger rollback request.
- Resolve a life trigger.
- Use a counter event.
- Resolve simultaneous triggers.
- Watch as spectator under default policy.
- Export and replay match.
- Report match from client.

## Bug policy
<!-- SECTION_REF: 11-testing-quality.s018 -->
Section Ref: `11-testing-quality.s018`

Every engine bug gets a regression test before the fix is merged.

Regression test name should include issue ID:

```text
tests/regressions/issue-123-on-ko-cancelled-incorrectly.test.ts
```

## CI stages
<!-- SECTION_REF: 11-testing-quality.s019 -->
Section Ref: `11-testing-quality.s019`

```text
1. Typecheck
2. Lint
3. Schema validation for card/effect definitions
4. Unit tests
5. Fuzz tests short run
6. Golden replay tests
7. Hidden-info tests
8. Integration tests
9. Build packages
```

Nightly CI can run longer fuzz tests and performance checks.

## Poneglyph/card-data tests
<!-- SECTION_REF: 11-testing-quality.s020 -->
Section Ref: `11-testing-quality.s020`

Add card-data tests once `@optcg/cards` exists:

```text
CD-001 Poneglyph response validates against Zod schema
CD-002 invalid Poneglyph shape fails before Redis cache write
CD-003 Redis cache key includes cardDataVersion and overlay/effect version
CD-004 overlay merge adds support status and effect definition IDs
CD-005 Poneglyph source text hash drift marks implementation stale
CD-006 unsupported non-vanilla Poneglyph card cannot enter ranked deck
CD-007 variant indexes/generated variant keys are accepted only when valid for the base card
CD-008 client display data cannot alter server-resolved match manifest
```

These tests prevent the card-data layer from becoming an implicit rules authority.


## v6 contract validation
<!-- SECTION_REF: 11-testing-quality.s021 -->
Section Ref: `11-testing-quality.s021`

Add these checks to CI before broad card implementation:

```bash
tsc -p contracts/tsconfig.json
# validate each card effect fixture against contracts/effect-dsl.schema.json
# run a SQL parser/linter against contracts/database-schema-v6.sql
# validate approved story files against contracts/story.schema.json
```

The fixture validator must reject deprecated DSL aliases such as `costOp`, `costValue`, `typeIncludes`, `cardNameNot`, and `colorIncludes` in committed canonical definitions. A migration script may accept them only when converting legacy examples.

Every rules-timing algorithm tightened in the v4, v5, and v6 spec passes must have at least one named test in `18-acceptance-tests.md` when code behavior is affected.


## Required repo enforcement linkage
<!-- SECTION_REF: 11-testing-quality.s022 -->
Section Ref: `11-testing-quality.s022`

The test strategy in this file is not complete unless it is wired into repository enforcement. The canonical repo-tooling requirements live in [`23-repo-tooling-and-enforcement.md`](23-repo-tooling-and-enforcement.md).

At minimum, CI must run linting, strict typechecking, package tests, contract/schema validation, and formatting checks before merge. Replay determinism and hidden-information regression lanes must be added before public alpha or ranked play.

A test expectation that is described in Markdown but not executable through local commands and CI is not considered fully implemented.
